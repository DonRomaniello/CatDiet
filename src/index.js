import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <div>These cats.</div>,
  document.getElementById('food') // make sure this is the same as the id of the div in your index.html
);
